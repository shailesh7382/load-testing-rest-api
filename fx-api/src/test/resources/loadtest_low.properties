baseUrl=http://localhost:8080/api

threads=1
requestsPerThread=2

baseline.threads=2
baseline.requestsPerThread=5

spike.threads=2
spike.requestsPerThread=10

soak.threads=1
soak.requestsPerThread=1
soak.durationSeconds=10

stress.threads=1
stress.requestsPerThread=2

sla.maxErrorRate=0.01
sla.p95LatencyMs=250
sla.minRps=50.0

