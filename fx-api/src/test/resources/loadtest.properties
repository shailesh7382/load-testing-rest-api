baseUrl=http://localhost:8080/api

threads=10
requestsPerThread=20

baseline.threads=2
baseline.requestsPerThread=5

spike.threads=50
spike.requestsPerThread=10

soak.threads=5
soak.requestsPerThread=5
soak.durationSeconds=120

stress.threads=100
stress.requestsPerThread=20

sla.maxErrorRate=0.01
sla.p95LatencyMs=250
sla.minRps=50.0

